# Repository workflow

- After completing and verifying any requested project modification, commit all changes made for that request and push the current branch to `origin`.
- Use a concise commit message that describes the completed change.
- Never force-push.
- Do not push a change whose relevant verification is failing; fix it first or clearly report the blocker.
- Preserve unrelated user changes and include only changes belonging to the current request.

